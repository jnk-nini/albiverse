import FeatureStub from "@/components/FeatureStub";

export default function PlannerPage() {
  return <FeatureStub title="Web Planner" eyebrow="CANON EVENTS • SHARED CALENDAR" description="A calendar pinned with dates, notes, reminders, and tiny future adventures." table="calendar_events" />;
}
