import ChapterLoadingScreen from "@/components/ChapterLoadingScreen";

export default function Loading() {
  return (
    <ChapterLoadingScreen
      chapter="Chapter 02 · Canon Countdowns"
      title="Pinning the milestones..."
      message="Tracing every date across the multiverse."
    />
  );
}
