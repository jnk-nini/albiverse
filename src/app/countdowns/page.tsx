import FeatureStub from "@/components/FeatureStub";

export default function CountdownsPage() {
  return <FeatureStub title="Canon Events" eyebrow="POST-ITS • COUNTDOWNS" description="Special events become editable notes that can be added, opened, updated, and removed." table="calendar_events" />;
}
