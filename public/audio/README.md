Place an audio file named `ambient.mp3` in this folder to enable the dashboard ambient music button.

Browsers require a user gesture before audio can play, so the app intentionally starts music only after the user clicks the volume button.
