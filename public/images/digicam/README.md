Place a transparent PNG named `chassis.png` in this folder to skin the Chapter 4
digicam with real chassis art. It's laid over the CSS-drawn camera body
(`.digicam-chassis-body` in `globals.css`) as a full-cover background image, so
if it's missing or fails to load, the pure-CSS camera chrome still renders fine.
